--[[
    RAER ULTIMATE CHEAT | V5 DEFINITIVE FIX (Silent Aim Update)
    Design: SKECH V2 (Dark/Red) - Premium
    Compatibilidade: Xeno Executor (R6 & R15)
]]

-- ================================================
-- VARIÁVEIS GLOBAIS (PARA GARANTIR FUNCIONAMENTO)
-- ================================================
getgenv().RAER_Settings = {
    -- AIMBOT
    Aim_Enabled = false,
    Aim_Key = Enum.UserInputType.MouseButton2,
    Aim_Part = "Head",
    Aim_FOV = 72,
    Aim_Smooth_V = 12,
    Aim_Smooth_H = 12,
    Aim_MaxDist = 240,
    Aim_VisibleOnly = true,
    Aim_IgnoreNPCs = true,
    Show_FOV = true,
    
    -- SILENT AIM (NOVO)
    Silent_Enabled = false,
    Silent_ShowFOV = false,
    Silent_FOV = 20,
    Silent_HitChance = 100,
    Silent_MaxDist = 203,
    Silent_MagicBullets = false,
    Silent_VisibleOnly = false,
    Silent_IgnoreNPCs = false,
    Silent_Key = Enum.UserInputType.MouseButton2,

    -- VISUALS
    ESP_Enabled = false,
    ESP_Box = false,
    ESP_Names = false,
    
    -- EXPLOITS
    Speed_Enabled = false,
    Walk_Speed = 100,
    Fly_Enabled = false,
    Fly_Speed = 80,
    Fling_Enabled = false,
    
    -- TELEPORT
    TP_Enabled = false,
    
    -- GERAL
    MenuKey = Enum.KeyCode.P
}

local Settings = getgenv().RAER_Settings

-- ================================================
-- VARIÁVEIS DE SISTEMA
-- ================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- ================================================
-- INTERFACE GRÁFICA (PREMIUM GUI V5 - FIXED)
-- ================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RAER_V5_Definitive"
ScreenGui.Parent = CoreGui or LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Size = UDim2.new(0, 650, 0, 450)
MainFrame.Position = UDim2.new(0.5, -325, 0.5, -225)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

-- Borda Vermelha Lateral
local AccentLine = Instance.new("Frame")
AccentLine.Parent = MainFrame
AccentLine.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
AccentLine.Size = UDim2.new(0, 2, 1, 0)
AccentLine.Position = UDim2.new(0, 140, 0, 0)
AccentLine.BorderSizePixel = 0

-- Sidebar
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Size = UDim2.new(0, 140, 1, 0)
Sidebar.BorderSizePixel = 0

local Logo = Instance.new("TextLabel")
Logo.Parent = Sidebar
Logo.Size = UDim2.new(1, 0, 0, 60)
Logo.BackgroundTransparency = 1
Logo.Text = "RAER"
Logo.TextColor3 = Color3.fromRGB(255, 0, 0)
Logo.Font = Enum.Font.GothamBold
Logo.TextSize = 28

-- Container de Abas
local TabContainer = Instance.new("Frame")
TabContainer.Name = "TabContainer"
TabContainer.Parent = MainFrame
TabContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
TabContainer.Position = UDim2.new(0, 150, 0, 10)
TabContainer.Size = UDim2.new(1, -160, 1, -20)
TabContainer.BackgroundTransparency = 1

local Pages = {}
local function CreatePage(name)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = TabContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 2
    Page.CanvasSize = UDim2.new(0, 0, 0, 600)
    local UIList = Instance.new("UIListLayout")
    UIList.Parent = Page
    UIList.Padding = UDim.new(0, 15)
    UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
    Pages[name] = Page
    return Page
end

local AimPage = CreatePage("Aim")
local SilentPage = CreatePage("SilentAim")
local VisualsPage = CreatePage("Visuals")
local MiscPage = CreatePage("Miscellaneous")
AimPage.Visible = true

-- Categorias na Sidebar
local function AddCategory(name, yPos)
    local label = Instance.new("TextLabel")
    label.Parent = Sidebar
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, yPos)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.fromRGB(100, 100, 100)
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
end

AddCategory("Aim", 70)
AddCategory("Visuals", 180)
AddCategory("Miscellaneous", 290)

local function AddTabButton(name, page, yPos)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.Size = UDim2.new(1, -10, 0, 30)
    btn.Position = UDim2.new(0, 5, 0, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    btn.BackgroundTransparency = 1
    btn.BorderSizePixel = 0
    btn.Text = "  " .. name
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        page.Visible = true
        for _, b in pairs(Sidebar:GetChildren()) do
            if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(200, 200, 200) end
        end
        btn.TextColor3 = Color3.fromRGB(255, 0, 0)
    end)
end

AddTabButton("Aimbot", AimPage, 95)
AddTabButton("Silent Aim", SilentPage, 125)
AddTabButton("Trigger Bot", AimPage, 155)
AddTabButton("Players", VisualsPage, 205)
AddTabButton("Vehicles", VisualsPage, 235)
AddTabButton("World", VisualsPage, 265)
AddTabButton("Exploits", MiscPage, 315)
AddTabButton("Teleport", MiscPage, 345)
AddTabButton("Configurações", MiscPage, 375)

local function CreateSection(parent, title, sizeY)
    local section = Instance.new("Frame")
    section.Size = UDim2.new(0.95, 0, 0, sizeY)
    section.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    section.BorderSizePixel = 1
    section.BorderColor3 = Color3.fromRGB(40, 40, 40)
    section.Parent = parent
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -10, 0, 25)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = title
    label.TextColor3 = Color3.fromRGB(150, 150, 150)
    label.Font = Enum.Font.Gotham
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = section
    return section
end

-- Funções Auxiliares de UI
local function AddCheckbox(parent, text, settingKey, yPos)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 25)
    btn.Position = UDim2.new(0, 10, 0, yPos)
    btn.BackgroundTransparency = 1
    btn.Text = (Settings[settingKey] and "✓ " or "  ") .. text
    btn.TextColor3 = Settings[settingKey] and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 13
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.Parent = parent
    btn.MouseButton1Click:Connect(function()
        Settings[settingKey] = not Settings[settingKey]
        btn.Text = (Settings[settingKey] and "✓ " or "  ") .. text
        btn.TextColor3 = Settings[settingKey] and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(200, 200, 200)
    end)
end

local function AddSlider(parent, text, settingKey, min, max, yPos, suffix)
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, yPos)
    label.BackgroundTransparency = 1
    label.Text = text .. "  " .. Settings[settingKey] .. (suffix or "")
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = parent
    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(0.9, 0, 0, 4)
    sliderBg.Position = UDim2.new(0.05, 0, 0, yPos + 22)
    sliderBg.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = parent
    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new((Settings[settingKey] - min) / (max - min), 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBg
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.Parent = sliderBg
    btn.MouseButton1Down:Connect(function()
        local moveConn
        moveConn = RunService.RenderStepped:Connect(function()
            local mousePos = UserInputService:GetMouseLocation().X
            local relativePos = mousePos - sliderBg.AbsolutePosition.X
            local percentage = math.clamp(relativePos / sliderBg.AbsoluteSize.X, 0, 1)
            local value = math.floor(min + (max - min) * percentage)
            Settings[settingKey] = value
            sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
            label.Text = text .. "  " .. value .. (suffix or "")
        end)
        local endConn
        endConn = UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                if moveConn then moveConn:Disconnect() end
                endConn:Disconnect()
            end
        end)
    end)
end

-- ================================================
-- ABA AIMBOT
-- ================================================
local AimConfig = CreateSection(AimPage, "Sobrescrever configuração", 100)
local PartLabel = Instance.new("TextLabel")
PartLabel.Size = UDim2.new(1, -20, 0, 20)
PartLabel.Position = UDim2.new(0, 10, 0, 30)
PartLabel.BackgroundTransparency = 1
PartLabel.Text = "Ponto de Acerto"
PartLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
PartLabel.Font = Enum.Font.Gotham
PartLabel.TextSize = 12
PartLabel.TextXAlignment = Enum.TextXAlignment.Left
PartLabel.Parent = AimConfig

local PartBtn = Instance.new("TextButton")
PartBtn.Size = UDim2.new(0.9, 0, 0, 25)
PartBtn.Position = UDim2.new(0.05, 0, 0, 55)
PartBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
PartBtn.Text = "Cabeça"
PartBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
PartBtn.BorderSizePixel = 0
PartBtn.Parent = AimConfig
PartBtn.MouseButton1Click:Connect(function()
    local parts = {["Cabeça"] = "Head", ["Peito"] = "Torso", ["Pé"] = "Feet"}
    local names = {"Cabeça", "Peito", "Pé"}
    local currentName = "Cabeça"
    for k, v in pairs(parts) do if v == Settings.Aim_Part then currentName = k end end
    local idx = table.find(names, currentName) or 1
    idx = idx + 1
    if idx > #names then idx = 1 end
    Settings.Aim_Part = parts[names[idx]]
    PartBtn.Text = names[idx]
end)

local AimGeneral = CreateSection(AimPage, "Geral", 80)
AddCheckbox(AimGeneral, "Ativar Assistencia de Mira", "Aim_Enabled", 30)
AddCheckbox(AimGeneral, "Mostrar Campo de Visao", "Show_FOV", 55)

local AimPrecision = CreateSection(AimPage, "Precisao", 180)
AddSlider(AimPrecision, "Campo de Visao", "Aim_FOV", 10, 800, 30)
AddSlider(AimPrecision, "Suavidade Vertical", "Aim_Smooth_V", 0, 50, 70)
AddSlider(AimPrecision, "Suavidade Horizontal", "Aim_Smooth_H", 0, 50, 110)
AddSlider(AimPrecision, "Distancia Maxima", "Aim_MaxDist", 50, 2000, 150, "m")

local AimMisc = CreateSection(AimPage, "Diversos", 80)
AddCheckbox(AimMisc, "Apenas Visiveis", "Aim_VisibleOnly", 30)
AddCheckbox(AimMisc, "Ignorar NPCs", "Aim_IgnoreNPCs", 55)

-- ================================================
-- ABA SILENT AIM (IMPLEMENTAÇÃO CONFORME IMAGEM)
-- ================================================
local SilentOverride = CreateSection(SilentPage, "Override config", 80)
local SilentKeyLabel = Instance.new("TextLabel")
SilentKeyLabel.Size = UDim2.new(1, -20, 0, 20)
SilentKeyLabel.Position = UDim2.new(0, 10, 0, 30)
SilentKeyLabel.BackgroundTransparency = 1
SilentKeyLabel.Text = "Tecla de Mira Silenciosa"
SilentKeyLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
SilentKeyLabel.Font = Enum.Font.Gotham
SilentKeyLabel.TextSize = 12
SilentKeyLabel.TextXAlignment = Enum.TextXAlignment.Left
SilentKeyLabel.Parent = SilentOverride

local SilentKeyBtn = Instance.new("TextButton")
SilentKeyBtn.Size = UDim2.new(0.9, 0, 0, 25)
SilentKeyBtn.Position = UDim2.new(0.05, 0, 0, 50)
SilentKeyBtn.BackgroundTransparency = 1
SilentKeyBtn.Text = "Right Mouse Button - Silent"
SilentKeyBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
SilentKeyBtn.Font = Enum.Font.Gotham
SilentKeyBtn.TextSize = 11
SilentKeyBtn.Parent = SilentOverride

local SilentGeneral = CreateSection(SilentPage, "Geral", 80)
AddCheckbox(SilentGeneral, "Ativar", "Silent_Enabled", 30)
AddCheckbox(SilentGeneral, "Mostrar Campo de Visao", "Silent_ShowFOV", 55)

local SilentPrecision = CreateSection(SilentPage, "Precisao", 150)
AddSlider(SilentPrecision, "Campo de Visao", "Silent_FOV", 0, 360, 30)
AddSlider(SilentPrecision, "Chance de Erro", "Silent_HitChance", 0, 100, 70, "%")
AddSlider(SilentPrecision, "Distancia Maxima", "Silent_MaxDist", 0, 1000, 110, "m")

local SilentMisc = CreateSection(SilentPage, "Diversos", 100)
AddCheckbox(SilentMisc, "Balas Magicas", "Silent_MagicBullets", 30)
AddCheckbox(SilentMisc, "Apenas Visiveis", "Silent_VisibleOnly", 55)
AddCheckbox(SilentMisc, "Ignorar NPCs", "Silent_IgnoreNPCs", 80)

-- ================================================
-- OUTRAS ABAS (VISUALS / MISC)
-- ================================================
local VisualsMain = CreateSection(VisualsPage, "Configurações de ESP", 120)
AddCheckbox(VisualsMain, "Ativar ESP", "ESP_Enabled", 30)
AddCheckbox(VisualsMain, "Caixas (Box)", "ESP_Box", 55)
AddCheckbox(VisualsMain, "Nomes", "ESP_Names", 80)

local MiscExploits = CreateSection(MiscPage, "Exploits", 150)
AddCheckbox(MiscExploits, "Ativar Speed Hack", "Speed_Enabled", 30)
AddSlider(MiscExploits, "Velocidade", "Walk_Speed", 16, 300, 55)
AddCheckbox(MiscExploits, "Ativar Fly", "Fly_Enabled", 100)
AddCheckbox(MiscExploits, "Car Fling (Joga Longe)", "Fling_Enabled", 125)

-- ================================================
-- LÓGICA DO AIMBOT & SILENT AIM
-- ================================================
local function GetTargetPart(character, partName)
    if not character then return nil end
    if partName == "Head" then return character:FindFirstChild("Head")
    elseif partName == "Torso" then return character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso")
    elseif partName == "Feet" then return character:FindFirstChild("LeftFoot") or character:FindFirstChild("RightFoot") or character:FindFirstChild("Left Leg") or character:FindFirstChild("Right Leg") end
    return nil
end

local AimFOV = Drawing.new("Circle")
AimFOV.Thickness = 1
AimFOV.Color = Color3.fromRGB(255, 0, 0)
AimFOV.Filled = false
AimFOV.Transparency = 0.5

local SilentFOV = Drawing.new("Circle")
SilentFOV.Thickness = 1
SilentFOV.Color = Color3.fromRGB(255, 0, 0)
SilentFOV.Filled = false
SilentFOV.Transparency = 0.5

local function GetClosestPlayer(fov, maxDist, visibleOnly, ignoreNPCs)
    local target = nil
    local shortestDistance = fov
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if ignoreNPCs and not player:IsA("Player") then continue end
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            if not hum or hum.Health <= 0 then continue end
            local targetPart = player.Character:FindFirstChild("Head") -- Silent Aim foca na cabeça por padrão
            if targetPart then
                local pos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local distToPlayer = (Camera.CFrame.Position - targetPart.Position).Magnitude
                    if distToPlayer > maxDist then continue end
                    local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Mouse.X, Mouse.Y + 36)).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        targe
(Content truncated due to size limit. Use line ranges to read remaining content)
