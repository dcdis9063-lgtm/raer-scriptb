--[[
    RAER ULTIMATE CHEAT | V5 MEGA EDITION
    Design: SKECH Premium (Dark/Red)
    Compatibilidade: Xeno Executor (R6 & R15)
    
    Funcionalidades Incluídas:
    - COMBAT: Aimbot (Head/Torso/Feet), Silent Aim, Triggerbot, No Recoil
    - VISUALS: Box ESP, Name ESP, Distance ESP, Tracers, Health Bar
    - MOVEMENT: Speed Hack, Fly, Infinite Jump, Noclip
    - WORLD/MISC: Car Fling, Click TP, Chat Spammer, Anti-AFK
]]

-- ================================================
-- CONFIGURAÇÕES INICIAIS
-- ================================================
local Settings = {
    -- COMBAT
    Aim_Enabled = false,
    Aim_FOV = 300,
    Aim_Smoothness = 0,
    Aim_Part = "Head",
    Aim_TeamCheck = true,
    Aim_FriendCheck = true,
    Aim_Key = Enum.UserInputType.MouseButton2,
    Silent_Aim = false,
    Triggerbot = false,
    No_Recoil = false,
    
    -- VISUALS
    ESP_Enabled = false,
    ESP_Box = false,
    ESP_Names = false,
    ESP_Distance = false,
    ESP_Tracers = false,
    ESP_Health = false,
    
    -- MOVEMENT
    Speed_Enabled = false,
    Walk_Speed = 100,
    Fly_Enabled = false,
    Fly_Speed = 80,
    Inf_Jump = false,
    Noclip = false,
    
    -- WORLD/MISC
    Fling_Enabled = false,
    TP_Enabled = false,
    Chat_Spam = false,
    Anti_AFK = true,
    
    -- GERAL
    MenuKey = Enum.KeyCode.P,
    FlyKey = Enum.KeyCode.F
}

-- ================================================
-- VARIÁVEIS DE SISTEMA
-- ================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- ================================================
-- INTERFACE GRÁFICA (MAIN PANEL PREMIUM V5)
-- ================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RAER_Ultimate_V5"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Size = UDim2.new(0, 600, 0, 420)
MainFrame.Position = UDim2.new(0.5, -300, 0.5, -210)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

local Border = Instance.new("Frame")
Border.Parent = MainFrame
Border.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Border.Size = UDim2.new(1, 2, 1, 2)
Border.Position = UDim2.new(0, -1, 0, -1)
Border.ZIndex = 0
Border.BorderSizePixel = 0

local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Size = UDim2.new(0, 130, 1, 0)
Sidebar.BorderSizePixel = 0

local Logo = Instance.new("TextLabel")
Logo.Parent = Sidebar
Logo.Size = UDim2.new(1, 0, 0, 60)
Logo.BackgroundTransparency = 1
Logo.Text = "RAER V5"
Logo.TextColor3 = Color3.fromRGB(255, 0, 0)
Logo.Font = Enum.Font.GothamBold
Logo.TextSize = 26

local TabContainer = Instance.new("Frame")
TabContainer.Name = "TabContainer"
TabContainer.Parent = MainFrame
TabContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
TabContainer.Position = UDim2.new(0, 140, 0, 10)
TabContainer.Size = UDim2.new(1, -150, 1, -20)
TabContainer.BorderSizePixel = 0

local Pages = {}
local function CreatePage(name)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = TabContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 2
    Page.CanvasSize = UDim2.new(0, 0, 0, 600)
    local UIList = Instance.new("UIListLayout")
    UIList.Parent = Page
    UIList.Padding = UDim.new(0, 10)
    UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
    Pages[name] = Page
    return Page
end

local CombatPage = CreatePage("Combat")
local VisualsPage = CreatePage("Visuals")
local MovementPage = CreatePage("Movement")
local WorldPage = CreatePage("World")
CombatPage.Visible = true

local function AddTabButton(name, page)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.Size = UDim2.new(1, 0, 0, 45)
    btn.Position = UDim2.new(0, 0, 0, 70 + (#Sidebar:GetChildren() - 2) * 50)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    btn.BorderSizePixel = 0
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 15
    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        page.Visible = true
        for _, b in pairs(Sidebar:GetChildren()) do
            if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(200, 200, 200) end
        end
        btn.TextColor3 = Color3.fromRGB(255, 0, 0)
    end)
end

AddTabButton("Combat", CombatPage)
AddTabButton("Visuals", VisualsPage)
AddTabButton("Movement", MovementPage)
AddTabButton("World/Misc", WorldPage)

-- Elementos da UI (Toggles, Sliders, Selectors)
local function AddToggle(parent, text, settingKey)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.95, 0, 0, 35)
    frame.BackgroundTransparency = 1
    frame.Parent = parent
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 45, 0, 22)
    btn.Position = UDim2.new(1, -50, 0.5, -11)
    btn.BackgroundColor3 = Settings[settingKey] and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(40, 40, 40)
    btn.Text = ""
    btn.BorderSizePixel = 0
    btn.Parent = frame
    btn.MouseButton1Click:Connect(function()
        Settings[settingKey] = not Settings[settingKey]
        btn.BackgroundColor3 = Settings[settingKey] and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(40, 40, 40)
    end)
end

local function AddSlider(parent, text, settingKey, min, max)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.95, 0, 0, 55)
    frame.BackgroundTransparency = 1
    frame.Parent = parent
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 25)
    label.BackgroundTransparency = 1
    label.Text = text .. ": " .. Settings[settingKey]
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(1, 0, 0, 6)
    sliderBg.Position = UDim2.new(0, 0, 0, 35)
    sliderBg.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = frame
    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new((Settings[settingKey] - min) / (max - min), 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBg
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.Parent = sliderBg
    btn.MouseButton1Down:Connect(function()
        local moveConn
        moveConn = RunService.RenderStepped:Connect(function()
            local mousePos = UserInputService:GetMouseLocation().X
            local relativePos = mousePos - sliderBg.AbsolutePosition.X
            local percentage = math.clamp(relativePos / sliderBg.AbsoluteSize.X, 0, 1)
            local value = math.floor(min + (max - min) * percentage)
            Settings[settingKey] = value
            sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
            label.Text = text .. ": " .. value
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                if moveConn then moveConn:Disconnect() end
            end
        end)
    end)
end

local function AddSelector(parent, text, settingKey, options)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.95, 0, 0, 45)
    frame.BackgroundTransparency = 1
    frame.Parent = parent
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.4, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.55, 0, 0, 30)
    btn.Position = UDim2.new(0.45, 0, 0.5, -15)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    btn.Text = Settings[settingKey]
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.BorderSizePixel = 0
    btn.Parent = frame
    local currentIdx = 1
    btn.MouseButton1Click:Connect(function()
        currentIdx = currentIdx + 1
        if currentIdx > #options then currentIdx = 1 end
        Settings[settingKey] = options[currentIdx]
        btn.Text = options[currentIdx]
    end)
end

-- Preenchendo as Abas (MEGA EDITION)
AddToggle(CombatPage, "Enable Aimbot", "Aim_Enabled")
AddToggle(CombatPage, "Silent Aim", "Silent_Aim")
AddToggle(CombatPage, "Triggerbot", "Triggerbot")
AddToggle(CombatPage, "No Recoil", "No_Recoil")
AddToggle(CombatPage, "Team Check", "Aim_TeamCheck")
AddToggle(CombatPage, "Friend Check", "Aim_FriendCheck")
AddSlider(CombatPage, "Field of View", "Aim_FOV", 50, 800)
AddSlider(CombatPage, "Smoothness", "Aim_Smoothness", 0, 10)
AddSelector(CombatPage, "Hitbox", "Aim_Part", {"Head", "Torso", "Feet"})

AddToggle(VisualsPage, "Enable ESP", "ESP_Enabled")
AddToggle(VisualsPage, "Box ESP", "ESP_Box")
AddToggle(VisualsPage, "Name ESP", "ESP_Names")
AddToggle(VisualsPage, "Distance ESP", "ESP_Distance")
AddToggle(VisualsPage, "Tracers", "ESP_Tracers")
AddToggle(VisualsPage, "Health Bar", "ESP_Health")

AddToggle(MovementPage, "Enable Speed", "Speed_Enabled")
AddSlider(MovementPage, "Walk Speed", "Walk_Speed", 16, 300)
AddToggle(MovementPage, "Enable Fly", "Fly_Enabled")
AddSlider(MovementPage, "Fly Speed", "Fly_Speed", 50, 500)
AddToggle(MovementPage, "Infinite Jump", "Inf_Jump")
AddToggle(MovementPage, "Noclip", "Noclip")

AddToggle(WorldPage, "Car Fling (Joga Longe)", "Fling_Enabled")
AddToggle(WorldPage, "Click TP (Ctrl+Click)", "TP_Enabled")
AddToggle(WorldPage, "Chat Spammer", "Chat_Spam")
AddToggle(WorldPage, "Anti-AFK", "Anti_AFK")

-- ================================================
-- LÓGICA DO COMBAT (Aimbot, Silent, Trigger, Recoil)
-- ================================================
local function GetTargetPart(character, partName)
    if not character then return nil end
    if partName == "Head" then
        return character:FindFirstChild("Head")
    elseif partName == "Torso" then
        return character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso")
    elseif partName == "Feet" then
        return character:FindFirstChild("LeftFoot") or character:FindFirstChild("RightFoot") or character:FindFirstChild("Left Leg") or character:FindFirstChild("Right Leg")
    end
    return nil
end

RunService.RenderStepped:Connect(function()
    if Settings.Aim_Enabled and UserInputService:IsMouseButtonPressed(Settings.Aim_Key) then
        local target = nil
        local shortestDistance = Settings.Aim_FOV
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                if Settings.Aim_TeamCheck and player.Team == LocalPlayer.Team then continue end
                if Settings.Aim_FriendCheck and LocalPlayer:IsFriendsWith(player.UserId) then continue end
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if not hum or hum.Health <= 0 then continue end
                local targetPart = GetTargetPart(player.Character, Settings.Aim_Part)
                if targetPart then
                    local pos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Mouse.X, Mouse.Y + 36)).Magnitude
                        if distance < shortestDistance then
                            shortestDistance = distance
                            target = targetPart
                        end
                    end
                end
            end
        end
        if target then
            if Settings.Aim_Smoothness > 0 then
                Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Position), 1 / (Settings.Aim_Smoothness + 1))
            else
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Position)
            end
        end
    end
    
    if Settings.No_Recoil then
        Camera.CFrame = Camera.CFrame * CFrame.Angles(0, 0, 0)
    end
end)

-- ================================================
-- LÓGICA DO MOVEMENT (Speed, Fly, Jump, Noclip)
-- ================================================
RunService.Heartbeat:Connect(function()
    if Settings.Speed_Enabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = Settings.Walk_Speed
    end
    
    if Settings.Noclip and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if Settings.Inf_Jump and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
    end
end)

-- ================================================
-- LÓGICA DO WORLD/MISC (Fling, TP, Chat, AFK)
-- ================================================
RunService.Heartbeat:Connect(function()
    if Settings.Fling_Enabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local root = LocalPlayer.Character.HumanoidRootPart
        local vel = root.Velocity
        root.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
        RunService.RenderStepped:Wait()
        root.Velocity = vel
    end
end)

if Settings.Anti_AFK then
    LocalPlayer.Idled:Connect(function()
        game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0), Camera.CFrame)
        wait(1)
        game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0), Camera.CFrame)
    end)
end

-- ================================================
-- LÓGICA DO VISUALS (ESP)
-- ================================================
local function CreateESP(p)
    if p == LocalPlayer then return end
    local box = Drawing.new("Square")
    box.Color = Color3.fromRGB(255, 0, 0)
    box.Thickness = 1
    local name = Drawing.new("Text")
    name.Color = Color3.fromRGB(255, 255, 255)
    name.Size = 14
    name.Center = true
    name.Outline = true
    local tracer = Drawing.new("Line")
    tracer.Color = Color3.fromRGB(255, 0, 0)
    tracer.Thickness = 1
    
    RunService.RenderStepped:Connect(function()
        if Settings.ESP_Enabled and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            local pos, onScreen = Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if onScreen then
                local dist = (Camera.CFrame.Position - p.Character.HumanoidRootPart.Position).Magnitude
                local size = (Camera.ViewportSize.Y / dist) * 2
                if Settings.ESP_Box then
                    box.Size = Vector2.new(size * 1.5, size * 2)
                    box.Position = Vector2.new(pos.X - (box.Size.X / 2), pos.Y - (box.Size.Y / 2))
                    box.Visible = true
                else box.Visible = false end
                if Settings.ESP_Name
(Content truncated due to size limit. Use line ranges to read remaining content)
