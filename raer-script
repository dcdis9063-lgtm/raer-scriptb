--[[
    RAER ULTRA CHEAT V3 | VERSÃO FINAL UNIFICADA
    Desenvolvido para: Xeno Executor
    
    Este é o código principal que você deve hospedar no seu link do GitHub.
    Comando para executar no Xeno:
    loadstring(game:HttpGet("SEU_LINK_RAW_AQUI"))()
    
    Funções:
    - Aimbot Lock-On (Cabeça, Peito e Pé Funcionando)
    - Speed Hack & Fly
    - ESP Box & Names
    - Painel Visual (Tecla P)
]]

-- ================================================
-- CONFIGURAÇÕES (Editáveis no VS Code)
-- ================================================
local Settings = {
    -- AIMBOT (MODO APELÃO)
    Aim_Enabled = true,
    Aim_FOV = 300,
    Aim_Part = "Head", -- "Head", "Torso", "Feet"
    Aim_TeamCheck = false,
    Aim_FriendCheck = true,
    Aim_VisibleCheck = false,
    Aim_Key = Enum.UserInputType.MouseButton2,
    
    -- ESP
    ESP_Enabled = true,
    ESP_Box = true,
    ESP_Names = true,
    
    -- FLY
    Fly_Enabled = false,
    Fly_Speed = 80,

    -- SPEED HACK
    Speed_Enabled = false,
    Walk_Speed = 100,
    
    -- GERAL
    MenuKey = Enum.KeyCode.P,
    FlyKey = Enum.KeyCode.F
}

-- ================================================
-- VARIÁVEIS DE SISTEMA
-- ================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- FOV Circle
local FOV_Circle = Drawing.new("Circle")
FOV_Circle.Thickness = 2
FOV_Circle.Color = Color3.fromRGB(255, 0, 0)
FOV_Circle.Filled = false
FOV_Circle.Transparency = 1
FOV_Circle.Visible = false

-- ================================================
-- FUNÇÃO PARA ENCONTRAR A PARTE CORRETA (FIXED)
-- ================================================
local function GetTargetPart(character, partName)
    if not character then return nil end
    
    if partName == "Head" then
        return character:FindFirstChild("Head")
    elseif partName == "Torso" then
        -- Compatibilidade R6 e R15 para o Peito
        return character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso")
    elseif partName == "Feet" then
        -- Compatibilidade R6 e R15 para os Pés
        return character:FindFirstChild("LeftFoot") or character:FindFirstChild("RightFoot") or character:FindFirstChild("Left Leg") or character:FindFirstChild("Right Leg")
    end
    return nil
end

-- ================================================
-- LÓGICA DO AIMBOT (LOCK-ON FORÇADO)
-- ================================================
local function GetClosestPlayer()
    local target = nil
    local shortestDistance = Settings.Aim_FOV
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if Settings.Aim_TeamCheck and player.Team == LocalPlayer.Team then continue end
            if Settings.Aim_FriendCheck and LocalPlayer:IsFriendsWith(player.UserId) then continue end
            
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            if not hum or hum.Health <= 0 then continue end
            
            local targetPart = GetTargetPart(player.Character, Settings.Aim_Part)
            if not targetPart then continue end
            
            local pos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
            if onScreen then
                local mousePos = Vector2.new(Mouse.X, Mouse.Y + 36)
                local distance = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                if distance < shortestDistance then
                    shortestDistance = distance
                    target = player
                end
            end
        end
    end
    return target
end

RunService.RenderStepped:Connect(function()
    if Settings.Aim_Enabled then
        FOV_Circle.Position = Vector2.new(Mouse.X, Mouse.Y + 36)
        FOV_Circle.Radius = Settings.Aim_FOV
        FOV_Circle.Visible = true
        
        if UserInputService:IsMouseButtonPressed(Settings.Aim_Key) then
            local target = GetClosestPlayer()
            if target and target.Character then
                local targetPart = GetTargetPart(target.Character, Settings.Aim_Part)
                if targetPart then
                    Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPart.Position)
                end
            end
        end
    else
        FOV_Circle.Visible = false
    end
end)

-- ================================================
-- LÓGICA DO SPEED HACK & FLY
-- ================================================
local bv = Instance.new("BodyVelocity")
local bg = Instance.new("BodyGyro")

RunService.Heartbeat:Connect(function()
    if Settings.Speed_Enabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = Settings.Walk_Speed
    end
end)

RunService.RenderStepped:Connect(function()
    if Settings.Fly_Enabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local root = LocalPlayer.Character.HumanoidRootPart
        bv.Parent = root
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bg.Parent = root
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bg.CFrame = Camera.CFrame
        local dir = Vector3.new(0,0,0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0,1,0) end
        bv.Velocity = dir * Settings.Fly_Speed
    else
        bv.Parent = nil
        bg.Parent = nil
    end
end)

-- ================================================
-- INTERFACE GRÁFICA (PAINEL RAER V3 FINAL)
-- ================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RAER_Panel_V3"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.Size = UDim2.new(0, 200, 0, 420)
MainFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.BorderSizePixel = 0

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 35)
Title.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Title.Text = "RAER CHEAT V3"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16

local Container = Instance.new("ScrollingFrame")
Container.Parent = MainFrame
Container.Position = UDim2.new(0, 5, 0, 40)
Container.Size = UDim2.new(1, -10, 1, -45)
Container.BackgroundTransparency = 1
Container.CanvasSize = UDim2.new(0, 0, 0, 550)
Container.ScrollBarThickness = 2

local UIList = Instance.new("UIListLayout")
UIList.Parent = Container
UIList.Padding = UDim.new(0, 5)

local function AddToggle(text, key)
    local btn = Instance.new("TextButton")
    btn.Parent = Container
    btn.Size = UDim2.new(1, 0, 0, 25)
    btn.BackgroundColor3 = Settings[key] and Color3.fromRGB(200, 0, 0) or Color3.fromRGB(40, 40, 40)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.BorderSizePixel = 0
    btn.MouseButton1Click:Connect(function()
        Settings[key] = not Settings[key]
        btn.BackgroundColor3 = Settings[key] and Color3.fromRGB(200, 0, 0) or Color3.fromRGB(40, 40, 40)
    end)
end

-- Botões
AddToggle("Aimbot Master", "Aim_Enabled")
AddToggle("Ignorar Amigos", "Aim_FriendCheck")
AddToggle("ESP Master", "ESP_Enabled")
AddToggle("ESP Box", "ESP_Box")
AddToggle("Fly (F)", "Fly_Enabled")
AddToggle("Speed Hack", "Speed_Enabled")

-- Seletor de Parte do Corpo
local PartLabel = Instance.new("TextLabel")
PartLabel.Parent = Container
PartLabel.Size = UDim2.new(1, 0, 0, 20)
PartLabel.BackgroundTransparency = 1
PartLabel.Text = "Mirar em: " .. Settings.Aim_Part
PartLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
PartLabel.Font = Enum.Font.Gotham

local function AddPartBtn(text, part)
    local btn = Instance.new("TextButton")
    btn.Parent = Container
    btn.Size = UDim2.new(1, 0, 0, 25)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.MouseButton1Click:Connect(function()
        Settings.Aim_Part = part
        PartLabel.Text = "Mirar em: " .. part
    end)
end

AddPartBtn("MIRA: CABEÇA", "Head")
AddPartBtn("MIRA: PEITO", "Torso")
AddPartBtn("MIRA: PÉ", "Feet")

-- Slider de FOV
local FOVLabel = Instance.new("TextLabel")
FOVLabel.Parent = Container
FOVLabel.Size = UDim2.new(1, 0, 0, 20)
FOVLabel.BackgroundTransparency = 1
FOVLabel.Text = "FOV: " .. Settings.Aim_FOV
FOVLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVLabel.Font = Enum.Font.Gotham

local FOVBtnPlus = Instance.new("TextButton")
FOVBtnPlus.Parent = Container
FOVBtnPlus.Size = UDim2.new(1, 0, 0, 25)
FOVBtnPlus.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
FOVBtnPlus.Text = "Aumentar FOV (+50)"
FOVBtnPlus.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVBtnPlus.Font = Enum.Font.Gotham
FOVBtnPlus.MouseButton1Click:Connect(function()
    Settings.Aim_FOV = Settings.Aim_FOV + 50
    FOVLabel.Text = "FOV: " .. Settings.Aim_FOV
end)

-- Atalhos
UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Settings.MenuKey then
        MainFrame.Visible = not MainFrame.Visible
    elseif input.KeyCode == Settings.FlyKey then
        Settings.Fly_Enabled = not Settings.Fly_Enabled
    end
end)

-- ESP OTIMIZADO
local function CreateESP(p)
    if p == LocalPlayer then return end
    local box = Drawing.new("Square")
    box.Color = Color3.fromRGB(255, 0, 0)
    box.Thickness = 1
    RunService.RenderStepped:Connect(function()
        if Settings.ESP_Enabled and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            local pos, onScreen = Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if onScreen then
                local dist = (Camera.CFrame.Position - p.Character.HumanoidRootPart.Position).Magnitude
                local size = (Camera.ViewportSize.Y / dist) * 2
                if Settings.ESP_Box then
                    box.Size = Vector2.new(size * 1.5, size * 2)
                    box.Position = Vector2.new(pos.X - (box.Size.X / 2), pos.Y - (box.Size.Y / 2))
                    box.Visible = true
                else box.Visible = false end
            else box.Visible = false end
        else box.Visible = false end
    end)
end
Players.PlayerAdded:Connect(CreateESP)
for _, p in ipairs(Players:GetPlayers()) do CreateESP(p) end

-- Limpar logs finais
print = function() end
warn = function() end
error = function() end
