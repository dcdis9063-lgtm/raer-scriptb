--[[
    RAER ULTIMATE CHEAT | V5 DEFINITIVE FIX (Xeno Compatible)
    Design: SKECH V2 (Dark/Red) - Premium
    Compatibilidade: Xeno Executor (R6 & R15) - Foco em execução sem falhas.
    
    Este script foi reescrito para garantir máxima compatibilidade e execução no Xeno,
    minimizando o uso de APIs problemáticas e focando na funcionalidade essencial.
]]

-- ================================================
-- VARIÁVEIS GLOBAIS (CONFIGURAÇÕES PADRÃO)
-- ================================================
getgenv().RAER_Settings = {
    -- SILENT AIM (Valores baseados na imagem)
    Silent_Enabled = false,
    Silent_ShowFOV = false,
    Silent_FOV = 20,
    Silent_HitChance = 0, -- 0x na imagem
    Silent_MaxDist = 203,
    Silent_MagicBullets = false,
    Silent_VisibleOnly = false,
    Silent_IgnoreNPCs = false,
    Silent_Key = Enum.UserInputType.MouseButton2, -- Right Mouse Button

    -- TRIGGER BOT (Valores baseados na imagem)
    Trigger_Enabled = false,
    Trigger_Smart = false,
    Trigger_ShowFOV = false,
    Trigger_FOV = 20,
    Trigger_MaxDist = 200,
    Trigger_Delay = 0,
    Trigger_VisibleOnly = false,
    Trigger_IgnoreNPCs = false,
    Trigger_Key = Enum.UserInputType.MouseButton1, -- Padrão para disparo

    -- ESP (Visuals)
    ESP_Enabled = false,
    ESP_Boxes = false,
    ESP_Names = false,
    ESP_Health = false,
    ESP_IgnoreTeammates = true, -- Nova opção para ignorar aliados

    -- Configurações de UI para fidelidade visual
    MenuKey = Enum.KeyCode.P,
    AccentColor = Color3.fromRGB(255, 0, 0),
    TextColor = Color3.fromRGB(200, 200, 200),
    ActiveTextColor = Color3.fromRGB(255, 0, 0),
    BackgroundColor = Color3.fromRGB(15, 15, 15),
    SidebarColor = Color3.fromRGB(10, 10, 10),
    SectionColor = Color3.fromRGB(20, 20, 20),
    SliderFillColor = Color3.fromRGB(255, 0, 0),
}

local Settings = getgenv().RAER_Settings

-- ================================================
-- VARIÁVEIS DE SISTEMA
-- ================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- ================================================
-- INTERFACE GRÁFICA (PREMIUM GUI V5 - FIDELIDADE VISUAL)
-- ================================================
-- Usando PlayerGui para maior compatibilidade com Xeno
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RAER_V5_Xeno_Definitive"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Settings.BackgroundColor
MainFrame.Size = UDim2.new(0, 650, 0, 450)
MainFrame.Position = UDim2.new(0.5, -325, 0.5, -225)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false

local AccentLine = Instance.new("Frame")
AccentLine.Parent = MainFrame
AccentLine.BackgroundColor3 = Settings.AccentColor
AccentLine.Size = UDim2.new(0, 2, 1, 0)
AccentLine.Position = UDim2.new(0, 140, 0, 0)
AccentLine.BorderSizePixel = 0

local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Settings.SidebarColor
Sidebar.Size = UDim2.new(0, 140, 1, 0)
Sidebar.BorderSizePixel = 0

local Logo = Instance.new("TextLabel")
Logo.Parent = Sidebar
Logo.Size = UDim2.new(1, 0, 0, 60)
Logo.BackgroundTransparency = 1
Logo.Text = "RAER"
Logo.TextColor3 = Settings.AccentColor
Logo.Font = Enum.Font.GothamBold
Logo.TextSize = 28

local TabContainer = Instance.new("Frame")
TabContainer.Name = "TabContainer"
TabContainer.Parent = MainFrame
TabContainer.BackgroundColor3 = Settings.BackgroundColor
TabContainer.Position = UDim2.new(0, 150, 0, 10)
TabContainer.Size = UDim2.new(1, -160, 1, -20)
TabContainer.BackgroundTransparency = 1

local Pages = {}
local ActivePage = nil

local function CreatePage(name)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = TabContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 2
    Page.CanvasSize = UDim2.new(0, 0, 0, 0)
    local UIList = Instance.new("UIListLayout")
    UIList.Parent = Page
    UIList.Padding = UDim.new(0, 15)
    UIList.SortOrder = Enum.SortOrder.LayoutOrder
    UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
    Pages[name] = Page
    return Page
end

-- Criar Abas
local AimbotPage = CreatePage("Aimbot")
local SilentAimPage = CreatePage("Silent Aim")
local TriggerBotPage = CreatePage("Trigger Bot")
local VisualsPage = CreatePage("Visuals")
local PlayersVisualsPage = CreatePage("PlayersVisuals") -- Nova página para Players dentro de Visuals
local MiscPage = CreatePage("Miscellaneous")

local TabButtons = {}
local function AddTabButton(name, page, yPos)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.Size = UDim2.new(1, -10, 0, 30)
    btn.Position = UDim2.new(0, 5, 0, yPos)
    btn.BackgroundTransparency = 1
    btn.BorderSizePixel = 0
    btn.Text = "  " .. name
    btn.TextColor3 = Settings.TextColor
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.TextXAlignment = Enum.TextXAlignment.Left
    
    btn.MouseButton1Click:Connect(function()
        if ActivePage ~= page then
            ActivePage.Visible = false
            page.Visible = true
            ActivePage = page
            for _, b in pairs(TabButtons) do
                b.TextColor3 = Settings.TextColor
            end
            btn.TextColor3 = Settings.ActiveTextColor
        end
    end)
    table.insert(TabButtons, btn)
    return btn
end

local function AddCategory(name, yPos)
    local label = Instance.new("TextLabel")
    label.Parent = Sidebar
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, yPos)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.fromRGB(100, 100, 100)
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
end

AddCategory("Aim", 70)
AddTabButton("Aimbot", AimbotPage, 95)
local silentAimBtn = AddTabButton("Silent Aim", SilentAimPage, 125)
local triggerBotBtn = AddTabButton("Trigger Bot", TriggerBotPage, 155)

AddCategory("Visuals", 180)
local playersVisualsBtn = AddTabButton("Players", PlayersVisualsPage, 205)
AddTabButton("Vehicles", VisualsPage, 235)
AddTabButton("World", VisualsPage, 265)

AddCategory("Miscellaneous", 290)

-- ================================================
-- CONTEÚDO DA ABA PLAYERS (VISUALS)
-- ================================================
local PlayersESP = CreateSection(PlayersVisualsPage, "ESP", 1)
AddCheckbox(PlayersESP, "Ativar ESP", "ESP_Enabled", 1)
AddCheckbox(PlayersESP, "Caixas", "ESP_Boxes", 2)
AddCheckbox(PlayersESP, "Nomes", "ESP_Names", 3)
AddCheckbox(PlayersESP, "Vida", "ESP_Health", 4)
AddCheckbox(PlayersESP, "Ignorar Aliados", "ESP_IgnoreTeammates", 5)
AddTabButton("Exploits", MiscPage, 315)
AddTabButton("Teleport", MiscPage, 345)
AddTabButton("Configuracoes", MiscPage, 375)

-- Definir página inicial
ActivePage = SilentAimPage
ActivePage.Visible = true
silentAimBtn.TextColor3 = Settings.ActiveTextColor

-- ================================================
-- FUNÇÕES DE CRIAÇÃO DE UI
-- ================================================
local function CreateSection(parent, title, layoutOrder)
    local section = Instance.new("Frame")
    section.Name = title
    section.Parent = parent
    section.BackgroundColor3 = Settings.SectionColor
    section.BorderSizePixel = 1
    section.BorderColor3 = Color3.fromRGB(40, 40, 40)
    section.Size = UDim2.new(0.95, 0, 0, 100) -- Tamanho ajustado pelo UIListLayout
    section.LayoutOrder = layoutOrder

    local UIList = Instance.new("UIListLayout")
    UIList.Parent = section
    UIList.Padding = UDim.new(0, 5)
    UIList.SortOrder = Enum.SortOrder.LayoutOrder
    UIList.HorizontalAlignment = Enum.HorizontalAlignment.Left
    
    local label = Instance.new("TextLabel")
    label.Parent = section
    label.Size = UDim2.new(1, -20, 0, 25)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = title
    label.TextColor3 = Color3.fromRGB(150, 150, 150)
    label.Font = Enum.Font.Gotham
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.LayoutOrder = 0
    
    return section
end

local function AddCheckbox(parent, text, settingKey, layoutOrder)
    local container = Instance.new("Frame")
    container.Parent = parent
    container.BackgroundTransparency = 1
    container.Size = UDim2.new(1, 0, 0, 25)
    container.LayoutOrder = layoutOrder

    local btn = Instance.new("TextButton")
    btn.Parent = container
    btn.Size = UDim2.new(1, -20, 1, 0)
    btn.Position = UDim2.new(0, 10, 0, 0)
    btn.BackgroundTransparency = 1
    btn.Text = (Settings[settingKey] and "✓ " or "  ") .. text
    btn.TextColor3 = Settings[settingKey] and Settings.ActiveTextColor or Settings.TextColor
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 13
    btn.TextXAlignment = Enum.TextXAlignment.Left
    
    btn.MouseButton1Click:Connect(function()
        Settings[settingKey] = not Settings[settingKey]
        btn.Text = (Settings[settingKey] and "✓ " or "  ") .. text
        btn.TextColor3 = Settings[settingKey] and Settings.ActiveTextColor or Settings.TextColor
    end)
end

local function AddSlider(parent, text, settingKey, min, max, layoutOrder, suffix)
    local container = Instance.new("Frame")
    container.Parent = parent
    container.BackgroundTransparency = 1
    container.Size = UDim2.new(1, 0, 0, 50)
    container.LayoutOrder = layoutOrder

    local label = Instance.new("TextLabel")
    label.Parent = container
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, 5)
    label.BackgroundTransparency = 1
    label.TextColor3 = Settings.TextColor
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left

    local sliderBg = Instance.new("Frame")
    sliderBg.Parent = container
    sliderBg.Size = UDim2.new(0.9, 0, 0, 4)
    sliderBg.Position = UDim2.new(0.05, 0, 0, 28)
    sliderBg.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    sliderBg.BorderSizePixel = 0

    local sliderFill = Instance.new("Frame")
    sliderFill.Parent = sliderBg
    sliderFill.BackgroundColor3 = Settings.SliderFillColor
    sliderFill.BorderSizePixel = 0

    local handle = Instance.new("Frame")
    handle.Parent = sliderBg
    handle.Size = UDim2.new(0, 8, 0, 8)
    handle.BackgroundColor3 = Settings.AccentColor
    handle.BorderSizePixel = 0

    local function UpdateSlider(value)
        local percentage = (value - min) / (max - min)
        sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
        handle.Position = UDim2.new(percentage, -4, 0.5, -4)
        label.Text = string.format("%s: %.1f%s", text, value, suffix or "")
        Settings[settingKey] = value
    end

    UpdateSlider(Settings[settingKey])

    local dragging = false
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
        end
    end)

    handle.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local mousePos = UserInputService:GetMouseLocation()
            local sliderStart = sliderBg.AbsolutePosition.X
            local sliderWidth = sliderBg.AbsoluteSize.X
            local percentage = math.clamp((mousePos.X - sliderStart) / sliderWidth, 0, 1)
            local value = min + (max - min) * percentage
            UpdateSlider(value)
        end
    end)
end

-- ================================================
-- CONTEÚDO DA ABA SILENT AIM
-- ================================================
local SilentOverride = CreateSection(SilentAimPage, "Override config", 1)
local SilentKeyLabel = Instance.new("TextLabel")
SilentKeyLabel.Parent = SilentOverride
SilentKeyLabel.Size = UDim2.new(1, -20, 0, 25)
SilentKeyLabel.Position = UDim2.new(0, 10, 0, 0)
SilentKeyLabel.BackgroundTransparency = 1
SilentKeyLabel.Text = "Tecla de Mira Silenciosa: Right Mouse Button - Silent"
SilentKeyLabel.TextColor3 = Settings.TextColor
SilentKeyLabel.Font = Enum.Font.Gotham
SilentKeyLabel.TextSize = 13
SilentKeyLabel.TextXAlignment = Enum.TextXAlignment.Left
SilentKeyLabel.LayoutOrder = 1

local SilentGeneral = CreateSection(SilentAimPage, "Geral", 2)
AddCheckbox(SilentGeneral, "Ativar", "Silent_Enabled", 1)
AddCheckbox(SilentGeneral, "Mostrar Campo de Visao", "Silent_ShowFOV", 2)

local SilentPrecision = CreateSection(SilentAimPage, "Precisao", 3)
AddSlider(SilentPrecision, "Campo de Visao", "Silent_FOV", 0, 360, 1, "")
AddSlider(SilentPrecision, "Chance de Erro", "Silent_HitChance", 0, 100, 2, "x")
AddSlider(SilentPrecision, "Distancia Maxima", "Silent_MaxDist", 0, 1000, 3, "m")

local SilentMisc = CreateSection(SilentAimPage, "Diversos", 4)
AddCheckbox(SilentMisc, "Balas Magicas", "Silent_MagicBullets", 1)
AddCheckbox(SilentMisc, "Apenas Visiveis", "Silent_VisibleOnly", 2)
AddCheckbox(SilentMisc, "Ignorar NPCs", "Silent_IgnoreNPCs", 3)

-- ================================================
-- CONTEÚDO DA ABA TRIGGER BOT
-- ================================================
local TriggerOverride = CreateSection(TriggerBotPage, "Override config", 1)
local TriggerKeyLabel = Instance.new("TextLabel")
TriggerKeyLabel.Parent = TriggerOverride
TriggerKeyLabel.Size = UDim2.new(1, -20, 0, 25)
TriggerKeyLabel.Position = UDim2.new(0, 10, 0, 0)
TriggerKeyLabel.BackgroundTransparency = 1
TriggerKeyLabel.Text = "Tecla de Disparo: OFF - Trigger"
TriggerKeyLabel.TextColor3 = Settings.TextColor
TriggerKeyLabel.Font = Enum.Font.Gotham
TriggerKeyLabel.TextSize = 13
TriggerKeyLabel.TextXAlignment = Enum.TextXAlignment.Left
TriggerKeyLabel.LayoutOrder = 1

local TriggerGeneral = CreateSection(TriggerBotPage, "Geral", 2)
AddCheckbox(TriggerGeneral, "Ativar", "Trigger_Enabled", 1)
AddCheckbox(TriggerGeneral, "Triggerbot Inteligente", "Trigger_Smart", 2)
AddCheckbox(TriggerGeneral, "Mostrar Campo de Visao", "Trigger_ShowFOV", 3)

local TriggerPrecision = CreateSection(TriggerBotPage, "Precisao", 3)
AddSlider(TriggerPrecision, "Campo de Visao", "Trigger_FOV", 0, 360, 1, "")
AddSlider(TriggerPrecision, "Distancia Maxima", "Trigger_MaxDist", 0, 1000, 2, "m")
AddSlider(TriggerPrecision, "Atraso", "Trigger_Delay", 0, 500, 3, "ms")

local TriggerMisc = CreateSection(TriggerBotPage, "Diversos", 4)
AddCheckbox(TriggerMisc, "Apenas Visiveis", "Trigger_VisibleOnly", 1)
AddCheckbox(TriggerMisc, "Ignorar NPCs", "Trigger_IgnoreNPCs", 2)

-- ================================================
-- LÓGICA DO CHEAT
-- ================================================
    local function GetClosestPlayer(fov, maxDist, visibleOnly, ignoreNpcs, ignoreTeammates, part)
        local closestPlayer, closestDist = nil, maxDist
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
                if ignoreTeammates and LocalPlayer.TeamColor == player.TeamColor then
                    continue
                end
                local targetPart = player.Character:FindFirstChild(part or "Head")
                if targetPart then
                    local pos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local dist = (Vector2.new(Mouse.X, Mouse.Y) - Vector2.new(pos.X, pos.Y)).Magnitude
                        if dist < fov and dist < closestDist then
                            closestPlayer, closestDist = player, dist
                        end
                    end
                en
(Content truncated due to size limit. Use line ranges to read remaining content)
